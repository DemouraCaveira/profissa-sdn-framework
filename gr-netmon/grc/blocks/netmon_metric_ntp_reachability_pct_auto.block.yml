id: netmon_metric_ntp_reachability_pct_auto
label: Metric ntp_reachability_pct (auto)
category: 'Network/Metrics/application'
flags: [python]
file_format: 1
documentation: 'NTP reachability'

parameters:
  - id: node
    label: Node
    dtype: string
    default: node
  - id: value
    label: Value
    dtype: real
    default: 0.0
  - id: interval
    label: Interval (s)
    dtype: real
    default: 1.0
  - id: jitter
    label: Jitter (+/-)
    dtype: real
    default: 0.0
  - id: labels_json
    label: Labels JSON
    dtype: string
    default: '{}'
  - id: details_json
    label: Details JSON
    dtype: string
    default: '{}'

inputs:
  - domain: message
    id: tick
    label: tick

outputs:
  - domain: stream
    id: out
    label: value
    dtype: float
    vlen: 1
  - domain: message
    id: metrics
    label: metrics

templates:
  imports: |
    from netmon.metric_emitter import MetricEmitterAuto
  make: |
    MetricEmitterAuto(metric_name='ntp_reachability_pct', layer='application', unit='%', node=${node}, value=${value}, interval=${interval}, jitter=${jitter}, labels_json=${labels_json}, details_json=${details_json})

ports:
  msg_in:
    - tick
  msg_out:
    - metrics
